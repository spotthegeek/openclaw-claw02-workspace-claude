# 2026-03-22

## Session with Mark

### Mission Control — Memory Module Fix
- Mark reported the Memory module in Mission Control wasn't working
- Root cause: `LocalOpenClawClient.DEFAULT_WORKSPACE` pointed to `/app/openclaw/` but memory files lived under `workspace/memory/` and `workspace-gamma/memory/`
- Fixed: Added per-agent workspace routing in `local_openclaw_client.py` via `_AGENT_WORKSPACE_MAP`
  - `main` → `/app/openclaw/workspace/memory/`
  - `gamma` → `/app/openclaw/workspace-gamma/memory/`
  - `claude` → `/app/openclaw/workspace-claude/` (root level — no `memory/` subdir)
- Updated all memory router routes to accept and forward `?agent_id=` query param
- Fixed frontend type mismatch: `MemoryFile.modifiedAt` vs `updated_at/created_at`
- Fixed `memory-list.tsx` grouping to use `modifiedAt` (was showing "Unknown")
- Fixed `MemoryViewer` to actually fetch file content via `useMemoryFile` hook (was displaying empty)
- Added agent switcher dropdown to memory page (Darcy/main, Gamma, Claude)
- Created missing `workspace-claude/memory/` directory

### Mission Control — Mobile Tasks View
- Mark reported bottom tasks inaccessible on mobile (viewport height issue)
- Kanban horizontal scroll also awkward on mobile
- Added `MobileTaskList` component: collapsible stacked sections per status column
- Tasks page auto-switches to compact mobile view below 768px
- Desktop retains full kanban with drag-and-drop
- Toggle button in header allows manual compact/normal switching

### Other
- Committed all changes to MC codebase (`a8803df`) and pushed to GitHub
- Created task #19: "Fix Claude agent memory processing" — assigned to Darcy, priority B, inbox

### Files Changed
- `backend/app/services/local_openclaw_client.py`
- `backend/app/routers/memory.py`
- `frontend/src/types/memory.ts`
- `frontend/src/hooks/use-memory.ts`
- `frontend/src/app/(dashboard)/memory/page.tsx`
- `frontend/src/components/memory/memory-list.tsx`
- `frontend/src/components/tasks/mobile-task-list.tsx` (new)
- `frontend/src/app/(dashboard)/tasks/page.tsx`
